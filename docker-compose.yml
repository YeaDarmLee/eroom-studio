version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_CONFIG=development
      - DATABASE_URL=sqlite:///eroom.db
    volumes:
      - .:/app
    command: python run.py
